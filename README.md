# pz-mod-updater
A simple bash script that uses the RCON command ‘checkModsNeedUpdate’ to check for mod updates and then restarts the Docker container

Tested and used with renegademaster/zomboid-dedicated-server
https://github.com/Renegade-Master/zomboid-dedicated-server
